# Launching Rocket Insurance

Clone this repo. Then after you clone the repo, cd into the `SureRocketInsurance` folder in your terminal and run `npm i`. Once everything is installed, run `npm start` and you are launched!
